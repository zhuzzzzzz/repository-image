#构建基于 EPICS base-7.0.7 开发环境的容器镜像: 
docker build -t base:dev .


#准备
0. 准备EPICS base



#测试
============== base-7.0.7:dev ==============
0.检查系统环境变量(EPICS_BASE, EPICS_HOST_ARCH, PATH)
1.检查softIoc命令是否正常运行
2.检查makeBaseApp.pl命令是否正常运行
3.运行第三方包命令(python, ping...)

